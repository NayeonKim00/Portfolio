# Output Files

This folder contains generated CSV outputs from the replication notebooks.

`monthly_panel_data.csv` is not included in the GitHub-ready folder because it is larger than GitHub's 100MB file limit. Run `../3. Fama-Macbeth.ipynb` to regenerate it locally.
